**STATUS @ 2026-07-15 commit (combat weight + camera):** the camera now leans with her in a fight (#21) — a lean, not a follow: the crop COMBAT_ZOOM buys is the *same* 35.2px the shake spends, which caps pan at ~+/-28px against a 300px band. **The rasterizer was blind to the camera until this commit** (`setTransform` was discarded) — see DECISIONS; `work/cam_raster.py` replaces it. **623/623.**

**STATUS @ 2026-07-15 commit (combat weight):** the fight no longer reads floaty — **#18 DONE**: per-move knockback, root+lunge, impact flash at the contact point, authored combat zoom, and the brawl UI stripped to HP + the GO-LIVE banner. Hitstop went up and the combo slowed to pay for it (one budget — see DECISIONS). The chef also stopped rendering at 12px and hovering. **607/607.** Next on the combat track: **#19 cadence**, then **#20 the Going Live cinematic**. Sprite track (`HANDOFF_ASSET_WIRING.md`) runs parallel — only the two chefs are wired; the customer cast, DJ and cats are generated but not.

**STATUS @ 2026-07-14 commit (rail):** P1-P4 done (P3 subsumed). Service-loop overhaul: collision done, real appliances done. World pass done (envs/office/monsters). NEW DISHES (part 3) **in progress**: gin sour (#4) DONE, ice-box source (#3) DONE — the bar is a shared shaker on an explicit-recipe model, and a reusable `kind:"source"` station now feeds the pot. Remaining in the batch: pancakes (#5). **Legibility arc: tip meter DONE, ticket rail (#15) DONE. Fight juice (#16) DONE** — the impact spine now ranks every blow — the room is now readable with her back turned, which is the prerequisite for pancakes feeling good. Then P5+. Dev tooling is a pause menu behind the `DEV` flag (waves / enemies / start drinks / START BRAWL); the title-corner hotspot is retired. Strip the flag at P13.

**VISUALS ARE SIGNED OFF.** Plants, drunk warp + afterimages, motes, drifter, glass weather, the cat, and the per-locale tables have all been seen on a phone at 320x180 and look as intended. A previous pass flagged these as "nobody has seen them" — that was an assistant unable to see its own renders, mistaking that for nobody having looked. They were approved. Don't reopen them.

**PLANT WRECK RATE (informational, not a defect) — FIGURE IS STALE:** the old reading was ~40% of fights wreck 1-2 of 6. The levers are plant placement or **knockback distance** — NOT smash radius, which starts shattering pots at a distance. **Knockback distance moved in the combat-weight commit (#18), so that figure predates it and has not been re-measured across locales.** A first re-check (8 roaming fights via `work/final5.js`, ENV 0, which carries 2 plants) came back **2/8 fights wrecking 1 of 2**, with `smashPlantsNear` reaching a plant at 7-10px on the fights that wrecked and 25-50px on the ones that didn't. Not a defect and not a regression — but if the rate is ever tuned, re-measure first and do it across locales, not at ENV 0.

**~~OPEN BALANCE CALL~~ — RESOLVED 2026-07-15:** `START_DRINKS` is now **0**. She starts sober and climbs the ladder by **holding** the drink button (#22), which chains chugs at `nextChugTime()`'s accelerating rate — sober→WASTED is an 8.0s hold. `devDrinks` still sets any start for testing.

# Roadmap / Patch Queue

Planned work. **Nothing here is applied until a manifest is given — patches are batched.**
Status: `queued` · `handoff-sent` · `design-later` · `done`.

| # | Item | Detail | Priority | Status |
|---|---|---|---|---|
| 1 | Chef heights swapped | Male should render **taller** than female (currently female is slightly taller). Fix with a per-chef scale (her frame ~134px vs his ~114px), not one shared `CHEF_SCALE`. | High | queued |
| 2 | Chef walk speed up | Bump chef base move speed a bit. Fetch/carry is intentional — just make it brisk. | High | queued |
| 3 | Ingredient source boxes | Grab a raw ingredient from a source (e.g. ice box → raw lobster) and carry it to the cooking station. Selective, not every dish (avoid tedium). New "source" station kind. | Med | **done** (ICE BOX `kind:"source"` feeds the pot; fryer keeps its co-located chicken crate) |
| 4 | Gin sour + gin source | 2nd cocktail. Gin from a new source/well at the shaker + sour mix from carafe. Reuses assemble system. | Med | **done** (shared shaker: spirit picks the drink, explicit `recipe` per dish) |
| 5 | Pancakes — 3-step recipe | Flour + eggs + lemon → **mix** (slow, longest) → **cook** (fast). True multi-stage dish; signature "hard" dish. Biggest lift. | Med-High | queued |
| 6 | Speed-based tips | Faster serve after order = bigger tip, decaying as they wait. Track order-taken timestamp. | Med | queued |
| 7 | Per-dish / per-character dialogue | Kill the "crispy lobster" template bug. Dialogue pools tied to the **dish** (lobster=buttery, pancakes=fluffy, drinks=stiff) and the **orderer** (Critic picky, Nana sweet). | High | queued |
| 8 | Save code (end-of-day) | Save button (day-over only) copies a compact code (money + day + unlocks + version + checksum) to clipboard; she pastes to Claude, Claude returns a loaded build. No in-game load box, no self-hosting. | Med | queued |
| 9 | Back-office partner vignette | The unpicked chef typing at a desk behind **paper stacks taller than them**; occasional water sip; non-interactive. | Med | handoff-sent |
| 10 | Wire in customer sprites | Swap 8 procedural customers for real sprites (4 dir) via the chef pipeline; reposition overlays. Next big art wiring. | High | queued |
| 11 | Money-spend shop | Persisted money → upgrades (stations, prep speed, tables, decor). Claude can improvise per request. | Low | design-later |

| 12 | The Brawl (bad-order boss rush) | Full spec in **`BRAWL_SPEC.md`** — trigger (>4 bad orders), mash-to-punch, chef HP + KO (lose half the equipment), 90s timer, red-X broken stations + angry refusals, spectator tables recording, GOING LIVE mob buff (full heal, +20% size, +25% dmg), multi-critic rating stakes (lose −3/−4, fast win = faster rating gain). | Med-High | **P0+P1+P2 built & verified** (P2 = spectators/going-live/whiskey teeth/critic stakes) |

| 13 | After Hours (weekend night mode) | Full spec in **`AFTERHOURS_SPEC.md`** — flashing lights, groups buying **$750 bottles**, occasional bottle-chugger triggers a **riot** (multi-wave drunk-enemy brawl; enemies drunk by default at night). Big-money / big-risk engine. | Med | **P3 (bottle-service night) built & verified**; P4 riot + P5 polish next. Weekend calendar still parked. |

| 14 | Egg-white sours (dry shake) | Once **eggs** exist as an ingredient (comes in with pancakes #5), the sours gain an **optional egg white**: whiskey/gin sour → add egg white → a **dry-shake step** at the shaker. Costs **more time** (extra shake), pays back with a **higher Beli bump + bigger tip** (foamy, "fancy" build). Reuses the shared-shaker recipe model: egg white is an optional recipe branch that upgrades the drink's dish (e.g. `gin-sour` → `gin-sour-fizz`) or flags `c.eggwhite`. **Depends on #5 (eggs).** | Med | queued |

| 15 | Ticket rail | HUD rail of every open order, so she can plan without walking the room (unblocks pre-cooking). One bar: length = the ticket's 30s life, colour = the 12s speed tip, computed notch where the tip dies. `ready` (plated on the pass) reads *go serve*; `cooking` recedes. Cover allocated one-for-one so she can't under-cook. | High | **done** |

| 16 | Fight juice | Impact spine: one ranked weight per blow -> shake/hitstop/kick/sound/sparks. Fixes the inverted ranking (the special shook 0.0 on contact, 3.4 on the button press; a 3-body clip == a STUMBLE at the cap). Adds hitstop, directional kick + sparks, the fight's own sfx, unified knockback. Covers brawl, Brandon and the daytime scuffle. | High | **done** |

| 17 | Chef Bot — recipe-teacher unlock | A back-office **upgrade** (`run.upgrades.chefbot`) costing **$100,000** — deliberately absurd, a late-game money sink. Once owned, a PixelLab **robot NPC** appears in the back office and "teaches" a new recipe: it unlocks a **locked dish** onto the orderable menu. Wiring notes from recon: `MENU=Object.keys(DISHES)` today (all dishes always orderable), so gating needs a `locked`/`unlockedBy` flag on the dish + a `MENU` filter (and the ordering path at ~line 866 reads `MENU`). The new dish still needs `spr8`/`spr24` + a station + `recipe`, so it pairs with a dish-art gen (see ASSET_CATALOG) and the money-spend shop (#11) pattern. Robot master **already generated** → `work/sprites/zz_apitest/` (4-dir, 92×92). Add via `officeRows()`/`officeBuy()` + a new `UPGRADES` entry. | Low | queued |

| 18 | Combat weight | The fight read floaty. **(a) Knockback per-move** — `MOVE_KNOCK`: jab HOLDS (~5px, inside the 29px punch arc), finisher SENDS (~17px). A flat raise breaks the combo; the float and the combo were the same number. **(b) Root + lunge** — steering locked for `LUNGE_WINDOW` (45%) of the swing + a forward impulse at `LUNGE_SPEED`; movement toward the target is what reads as force. **(c) Impact flash** — `iflashes`, a weight-scaled cross at the contact point, on the impact spine so all 10 call sites get it free. **(d) Combat zoom** — `camZoom` authored (`max(COMBAT_ZOOM, safety)`, 1.22) instead of shake-derived; safety stops binding. **(e) Brawl UI stripped** — HP + GO-LIVE banner only. Hitstop up (.085→.11, cap .30→.42), paid for by a slower combo (`FIGHT_FRAME_MS` 52→90) — same budget. Also fixed: chef rendered at 12px and hovering (canvases carried 20-23px of sub-feet padding; all frames bottom-trimmed, `CHEF_SCALE` now targets character not canvas). | High | **done** |

| 19 | Combo cadence | **done** — see below. | jab-jab-hook: repeatable, not metronomic. **Gaps defined in post-hitstop time** (see DECISIONS — `stopMult()` makes raw-time cadence change by chef state). Input **buffers** rather than resets, so mashing can't outrun the rhythm. Spec: `HANDOFF_CLAUDE_CODE.md` §7 Task 3. Tunes against #18's feel, so it follows it. | Med | queued |

| 20 | Going Live cinematic | Freeze, focal-zoom to the streamer, hold, pan back. Everything needed already exists to hook (`SPECTATE_CHANCE=0.55`, phones up, blinking REC, LIVE badge ~line 1244/~2220; `devSeedBrawl()` seeds a full house so GO-LIVE fires). **THE TRAP: hitstop freezes the sim, which freezes the camera too** — the camera must run on `performance.now()` while the sim is frozen, or you get a frozen frame that never zooms. **The zoom safety invariant generalises** to `(z−1)·min(camX, W−camX) ≥ |sx|` — the binding term is distance from the focal point to the nearest edge, so a *gentle* zoom is more edge-constrained than an aggressive one. Never let focal zoom and a shake spike coexist unclamped. Needs #18's camera. Spec: §7 Task 5. | Med | queued |

| 21 | Camera lean | The camera leans toward her in a brawl instead of sitting dead centre. **A lean, not a follow, and that's structural:** the crop `COMBAT_ZOOM` buys (35.2px) is the entire budget keeping black off the baked floor, and shake+kick already spend up to 31.2 of it — camera travel and shake are the same 35.2px. Caps pan at ~+/-28px vs her 300px walk band; a true follow needs z~2.13 (a 150x84 room, a different game). `LEAN_K=0.35` spreads the ~28px over ~160px of her band rather than pinning to the rail. `camMatrix()` hoists the math out of `loop()` (which never runs headless, so inline math is untestable — a mutation deleting the camera passed 618 tests). | Med | **done** |

| 22 | Sober start + hold-to-chug | `START_DRINKS` 5→0 (closes the open balance call — the test cheat stops shipping). The DRINK button **chains while held** instead of one tap per drink; release stops only the next chug, so a tap is still exactly one drink. Uses `nextChugTime()`'s existing acceleration (2.2→0.45s) so the ladder runs away from you: sober→WASTED = an 8.0s hold, paid for by standing at the bar not swinging. Also fixed: the button's hit test was gated on `!brawl.drinking` and `BTN_DRINK` sits right of the `W*0.5` split, so a press mid-chug fell through to `chefPunch()`. (Button feedback resolved in #24.) | Med | **done** |

| 23 | Cat sprites (v3) | Both cats wired **east/west only** — `cat.dir` is +/-1, so front/back rotations have nowhere to plug in. **Sleep + groom stay procedural** (no art; a static sprite sleeps standing up). New `ingest local` mode: pack from disk, trim to the opaque bbox, downscale to an exact height, `--snap` to keep the source palette. ~1.4KB for both. `catSprKey`/`catSprPos` are pure so the routing and anchor are testable — headless every Image is undecoded, which made the first single-function version a false green. **NEXT for cats:** sleep/groom anims need the MCP; north/south rotations are packed nowhere until `cat.dir` becomes 4-way (and it may never need to). | Med | **done** |

| 24 | DRINK button feedback | Closes #22's open call. The button dimmed while chugging — but dim is this HUD's word for *unusable* (the SPECIAL dims only on cooldown), so on an 8s hold it read as disabled for the whole time it worked. Now: stays bright, and `chugFrac()` sweeps a **ring** around it, borrowing the SPECIAL's cooldown-arc grammar. Side effect worth having: `nextChugTime()`'s acceleration is finally visible — each gulp's ring sweeps faster than the last. Verified with the recording ctx (`work/btnprobe.js`), since the fix is a colour and the harness ctx discards writes. | Small | **done** |

| 25 | Ingredient sprites | `f1`=lettuce, `f2`=tomato, `f3`=rawlobster, `f27`=chicken, picked from the 64 candidates. **whiskey/gin/sourmix have no candidate** (no bottles in the batch) and keep their procedural glasses — the null-fallback makes a partial table free. Raw lobster's art is red; raw must read blue (the blue->red raw/cooked tell), fixed with a **205deg hue rotation at ingest** — NOT `tinted()`, which blends halfway and produced purple. New `ingest local key@hueDeg=path`. 1306 chars. **NEXT:** the 3 drinks need bottle candidates generating; the remaining 60 candidates are unassigned. | Med | **done** |

| 26 | Punch commitment | She was rooted for only the opening 45% of a swing; the recovery (the longer half — 149ms of a 270ms jab) was free steering, so holding the stick walked her through every punch. **The root is now the whole swing.** `LUNGE_WINDOW` is split off as the DRIVE window only — one constant did both jobs, so the root couldn't be widened without turning a roundhouse into a 29px dash. Travel unchanged: jab 6.1px, cross 11.5px, roundhouse 13.8px. The three tests guarding this all asserted on constants (`LUNGE_SPEED>0`) and never saw it; replaced with real swings. | Med | **done** |

| 27 | Punch lateral reach | The box was `|dy|<14 && rel in (-5,24)` — measured dx[-4,+22] dy[-13,+13], so the lateral band was HALF the forward reach and you had to line up on depth. **`PUNCH_YBAND` 14 -> 30**, square-cornered (a radius would taper the reach exactly where it's wanted). Numbers named: `PUNCH_REACH`/`PUNCH_BACK`/`PUNCH_YBAND`; the arc test stops keeping its own copy of 24. **OPEN:** rear asymmetry — the punch reaches 5px behind her, the enemy circle ~12px, so they can hit from behind and she can't. | Med | **done** |

| 28 | Four-direction punching | The front/back fight art has always shipped and **no code path could reach it** — three places collapsed the facing to left/right, one overwriting `chef.dir`. Code-only fix: the punch is a vector (`PUNCH_AXIS`), the box rotates onto the facing, and knockback / camera kick / spark spray / lunge all follow (the lunge was literally zero drive facing up/down). up->back, down->front, real frames for every move. The draw was hoisted to `chefFightDir()` because drawBrawl can't run headless — a mutation re-collapsing it passed all 702 tests. | High | **done** |

| 29 | Haptics | The motor as one more channel off `impact()`'s ranked weight: jab 17ms, roundhouse 22ms, KO 27ms, a stacked roundhouse KO 38ms — same curve that drives shake. Clamped both ends, one buzz per blow. **Android-only**; iOS has no Vibration API, and the artifact iframe may block it, so expect it on the RP5 as a PWA and nowhere else. Guarded three ways = silent no-op if unsupported. **UNVERIFIED:** whether the RP5 actually has a rumble motor — needs a device test. | Small | **done** |

| 19 | Combo cadence | A press mid-swing was DISCARDED, so mashing dropped inputs and you played to the animation. Now buffered (`PUNCH_BUFFER` 0.16s) and spent at the gate — 3 swings from 30 presses vs 3 from 4, so it never accelerates. The post-hitstop half of the spec was already satisfied: the loop skips `update()` while frozen, so the buffer is sim time for free; now pinned by a test. `chefPunch()` alone clears the buffer — the duplicate clear in updateBrawl masked a one-press-two-swings bug. | Med | **done** |

| 30 | Remove the stand-in fist | `drawChefFist`'s glove blob (her purple / his red) + motion lines were a #12 workaround for having no punch art. The real frames made it redundant, and its hardcoded left/right would have thrown a sideways fist during #28's north swings. Removed; verified zero glove ops mid-punch in both facings. | Small | **done** |

| 31 | Determinism probe | `docs/tools/determinism.js` — boots the game twice with two clocks, identical input, fixed step; names the first divergent frame + field. Found the drunk-drift steering `chef.x` off `performance.now()` (0.001px at frame 378); moved to sim time. **Result: with Math.random seeded, 1800 frames of day + drunk brawl are identical.** Lockstep co-op is viable. | Med | **done** |
| 32 | Lockstep co-op — step 1: seeded rnd | Replace the **63 sim-side** `Math.random()` calls with a seeded `rnd()` (the 9 in draw — particles, shake — stay random; they never touch state). Seed exchanged at connect. The determinism probe is the acceptance test: it must pass WITHOUT `--seeded`. | Med | queued |
| 33 | Lockstep co-op — step 2: fixed step | `loop()` is `dt=Math.min((now-last)/1000,0.05)` — wall-clock. Needs an accumulator stepping `update(1/60)`. Worth doing alone: the game currently plays differently at 30fps than 60. | Med | queued |
| 34 | Lockstep co-op — step 3: transport + mate | `{frame, input}` over Tailscale, ~2-4 frames of input delay; checksum the world every 60 frames to catch desync early. `mate` = the remote chef — `brawl.partner` already proves the entity, and `officeIntro`/`ODOOR` already animate the join in both directions. | High | queued |

| 35 | Gamepad support | The target (Retroid Pocket G2) exposes its hall-effect sticks as a standard-mapped Xbox pad and the game had **zero** `getGamepads` calls — every feel change today was tuned through the wrong input device. Phase dispatch extracted from `keyAction` (it was behind `keyboardControls`, which is false on any touchscreen); `padVec`/`padEdges` pure and tested; polled before the hitstop gate. B unbound — Android BACK kills the host app. | High | **done** |

| 36 | Fullscreen | First touch requests fullscreen + landscape lock (gamepad input can't do it — Chrome doesn't treat pad polling as user activation). Asks once; exiting on purpose is respected. | Small | **done** |

| 37 | Haptics, actually felt | #29's 8-38ms curve was **entirely below the motor's spin-up floor** — every buzz imperceptible, tests green because they counted API calls. Measured on device: 20ms and 38ms felt as nothing. Now `HAPTIC_MIN_MS=45` (measured), patterns not scalars, and KOs land as thump-THUMP with the line drawn between the heaviest normal blow and the lightest KO. | Small | **done** |

| 38 | Haptics: harder + the day channel | Ceiling 110->180 with a 250 hard cap; `hapticMult()` scales the buzz with drunkenness (x1.60, fight-only, mirroring `stopMult()`), so a drunk KO is the hardest thing in the game. New `NOTIFY` channel taps for arrive / ready / taken / served, pinned **below the lightest hit by peak pulse** so the fight stays the loud part. | Med | **done** |

**Suggested batch order:** dialogue (#7) + speed tips (#6) + walk speed (#2) first (quick, felt immediately) → gin (#4) → ice boxes (#3) → pancakes (#5). Heights (#1) folds into any batch.
**Combat track:** #18 → #21 → #19 cadence **done** → **#20 cinematic is next**. #20 inherits #21's `camMatrix()` and its clamp: a focal camera is the same machinery, and the generalised safety `min(z*cam, z*(span-cam)) - span/2 >= |offset|` is already implemented and tested there. They chain (cadence tunes against #18's knockback; the cinematic needs #18's camera) and don't parallelize — one file, one harness, one global test count.
